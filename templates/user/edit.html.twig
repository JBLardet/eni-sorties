{% extends 'base.html.twig' %}

{% block title %}Modifier le profil : " {{ user.pseudo }} "{% endblock %}

{% block body %}
    {{ include('user/_photo.html.twig') }}
    <h1>Modifier le profil " {{ user.pseudo }} "</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Modifier'}) }}
    {% if is_granted("ROLE_ADMIN") %}
        {{ include('user/_delete_form.html.twig') }}
        <a href="{{ path('user_index') }}"><button class="btn enligne" value="Liste de tous les utilisateurs">Liste de tous les utilisateurs</button></a>
    {% endif %}
{% endblock %}
