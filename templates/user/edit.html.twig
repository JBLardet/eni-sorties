{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <h1>Modifier le profil : {{ user.pseudo }}</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Modifier'}) }}

    {% if is_granted("ROLE_ADMIN") %}
        {{ include('user/_delete_form.html.twig') }}
    {% endif %}
{% endblock %}
